<%
  layout = false;
  const isLoggedIn = typeof user !== 'undefined' && user;
%>
<%# Primary function: show the home page with all history boards for the logged-in user. %>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home - The History Board</title>
  <link href="/styles.css" rel="stylesheet">
</head>
<body>
  <%# Navigation bar for the home page, showing links based on login state. %>
  <nav>
    <div class="nav-brand">The History Board</div>
    <% if (isLoggedIn) { %>
      <div class="nav-links">
        <a href="/">Home</a>
        <a href="/create-post" class="btn">+ New History Board</a>
        <div class="nav-user">
          Welcome, <strong><%= username %></strong>
          <a href="/logout">Logout</a>
        </div>
      </div>
    <% } else { %>
      <div class="nav-links">
        <a href="/login">Login</a>
        <a href="/signup">Sign Up</a>
      </div>
    <% } %>
  </nav>

  <div class="container">
    <%# Container for the list of boards and the empty state message. %>
    <h1>All Boards</h1>

    <% if (posts && posts.length > 0) { %>
      <%# Loop through each board and render a summary card. %>
      <% posts.forEach(post => { %>
        <div class="card" onclick="window.location.href='/post/<%= post.id %>'">
          <div class="post-title"><%= post.title %></div>
          <div class="post-meta">
            by <strong><%= post.username %></strong>
            on <%= new Date(post.created_at).toLocaleDateString() %>
          </div>
          <div class="post-content"><%= post.content %></div>
          <a href="/post/<%= post.id %>" class="post-card-link">Read more →</a>
          
          <% if (isLoggedIn && user === post.user_id) { %>
            <div class="post-actions">
              <form method="POST" action="/delete-post/<%= post.id %>" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this board?');">
                <button type="submit" class="btn btn-danger">Delete</button>
              </form>
            </div>
          <% } %>
        </div>
      <% }); %>
    <% } else { %>
      <%# Show this message when there are no boards yet. %>
      <div class="empty-state">
        <p>The annals of history await your contribution. Be the first to create a history board!</p>
        <% if (isLoggedIn) { %>
          <a href="/create-post" class="btn">Create Your First History Board</a>
        <% } else { %>
          <a href="/signup" class="btn">Join the Historical Discourse</a>
        <% } %>
      </div>
    <% } %>
  </div>
</body>
</html>
